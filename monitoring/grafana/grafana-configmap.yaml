apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
data:
  grafana.ini: |
    [server]
    http_port = 3000
    root_url = /grafana
    [security]
    admin_user = admin
    admin_password = admin
